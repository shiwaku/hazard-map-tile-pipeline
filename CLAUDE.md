# CLAUDE.md

## プロジェクト概要

ハザードマップ（浸水想定区域）のシェープファイル・GeoJSON から、産総研
「グリッドPNGタイル」仕様に適合する XYZ ラスタータイル（パレットPNG／数値PNG）を
生成するパイプライン。検査 → 前処理 → ラスタライズ → 符号化 → タイル生成 →
メタデータを設定ファイル 1 枚で通す。

姉妹リポジトリ: [aerial-photo-tile-pipeline](https://github.com/shiwaku/aerial-photo-tile-pipeline)（航空写真版）。
スクリプト構成・設定ファイル方式・`auto` 解決の考え方はそちらに揃えている。
`viewer/` は [mlit-urban-planning-converter](https://github.com/shiwaku/mlit-urban-planning-converter)
のビューワ（Vite + TypeScript）を土台に、レイヤー部分だけラスタータイル用に差し替えたもの。

詳細な使い方・設計判断は `README.md` を参照。

## 取り扱いルール（重要）

- **このリポジトリで扱う検証データは公開データ（オープンデータ）のみ。**
  業務で受領したデータ・顧客提供データ・案件固有の受領仕様や実測値は
  コード・ドキュメント・コミットメッセージ・設定例のいずれにも含めない。
- 自治体名・河川名・属性名などが業務案件の受領データ由来である場合は書かない。
  同じ知見を書きたい場合は、**公開データで再現・確認したうえで**、
  公開データを根拠として一般論の形で記述する。
- `data/` と `output/` は `.gitignore` 済み。案件ごとの設定 `config/*.conf` も
  コミット対象外（`config/*.conf.example` のみ管理）。
- **`config/*.conf.example` に足した構成は GitHub Pages で全世界に公開される。**
  CI（`.github/workflows/pages.yml`）が `DATASETS` に並べた構成をパイプラインに通し、
  生成タイルを Pages にデプロイするため。案件データを example に書かないこと。
  公開したくない構成は `DATASETS` に入れない。

## 構成

| パス | 役割 |
|------|------|
| `scripts/00_fetch_data.sh` | Step 0（任意）: サンプルデータの ZIP 取得と展開 |
| `scripts/01_inspect.sh` … `05_make_metadata.sh` | 各ステップ。第1引数に設定ファイルを取る |
| `scripts/run_pipeline.sh` | Step 1〜5 の一括実行（`--from N` / `--to N`） |
| `scripts/serve.sh` | ローカルプレビュー（`viewer/index.html` を配信） |
| `scripts/build_site.sh` | 公開用サイトの組み立て（`_site/`）。タイルとビューワだけを集める |
| `.github/workflows/pages.yml` | 同梱サンプルを通して GitHub Pages に公開する CI |
| `scripts/lib/common.sh` | 設定ロード・既定値・ログ・依存チェック・入力列挙 |
| `tools/inspect_inputs.py` | 入力検査・`auto` 値の解決（stdlib + `osgeo`） |
| `tools/rankdef.py` | ランク定義 → カラーマップ / 凡例JSON / 分類SQL（stdlib のみ） |
| `tools/encode_numeric.py` | 実数値ラスター → 数値PNGの RGBA（numpy + `osgeo`） |
| `tools/make_metadata.py` | tiles.json / legend.json（stdlib のみ） |
| `tools/validate_tiles.py` | 生成タイルの仕様適合検証（Pillow + numpy） |
| `tools/fetch_data.py` | ZIP 取得と CP932 ファイル名の展開（stdlib のみ） |
| `tools/make_dataset_index.py` | output/ を走査して datasets.json を作る（stdlib のみ） |
| `colors/*.json` | ランク定義。値・しきい値・配色・出典を 1 か所にまとめる |
| `viewer/src/layers.ts` | タイルセットの読込・凡例・画素→値の復号。**ここがラスタ用の差し替え部分** |
| `viewer/src/main.ts` | 地図・UI・クリックでの画素読み取り |
| `viewer/src/{basemap,theme}.ts`, `pale-style.json`, `style.css` | 土台からほぼそのまま流用 |

## 設計上の決めごと

- **出力形式は入力データの性質で決まる。** 災害種別ではない。
  離散ランク → パレットPNG のみ（ランクから実数は復元できないので numeric は禁止し、
  明示指定されたらエラーで止める）。実数値 → パレットPNG も数値PNG も可
  （`auto` の既定は numeric）。この分岐が `01_inspect.sh` の中心。
- **`auto` 値の解決は Step 1 の検査結果を唯一の根拠にする。**
  `SRC_SRS` / `VALUE_FIELD` / `VALUE_KIND` / `TILE_TYPE` / `MESH_SIZE` / `MAX_ZOOM`
  は `auto` を受け付ける。解決結果は `inspect/inputs.json` に書き、Step 2 以降は
  そこだけを読む。各ステップがバラバラに判定しないこと。
- **縮小方法は `near` から変えない。** パレットPNGはピクセル値から凡例を引き、
  数値PNGは RGB を 24 ビット整数として解釈する。どちらもチャンネルを独立に
  混ぜられると壊れる。`near` は全バンドが同じ元ピクセルを採るので崩れない。
  実測: 同じ入力を `average` で通すと凡例外の色が 347 種類 / 163,963px 出た
  （ZL9〜18 の全レベル。gdal2tiles は最大ZLの生成にも `-r` を使うため）。
  `mode` もバンドごとに独立計算されるので使わない。
- **メッシュサイズは頂点座標の間隔の最頻値から実測する。** 浸水想定区域の
  ポリゴンはメッシュ境界に沿うので、頂点座標はメッシュ間隔の格子に載る。
  実測値は細分メッシュの規定値（ガイドライン共通編 表10）に突き合わせ、
  **ラスタライズの格子は常に規定値（秒）に載せる**。元データが平面直角座標系でも
  出力は標準の格子に揃う。
- **最大ZL**: タイル解像度がメッシュ1辺に最も近い ZL（ネイティブZL）
  `round(log2(res(0) / mesh))`（`res(0) = 156543.033928 × cos(φ)`）に
  `MAX_ZOOM_MARGIN`（既定3）を足し、`MAX_ZOOM_CAP`（既定18）でクリップ。
  5m メッシュなら ZL18 になり「原則ZL18」の運用と一致する。
- **ラスタライズ後に nearest で `UPSCALE` 倍（既定2）に拡大する。**
  メッシュ解像度のままだと最大ZL超のズームで境界がぼやける。
  bilinear 等を使うと隣接ランクの色（＝値）が混ざるので使わない。
- **値属性は Step 2 で `_value` に正規化する。** 属性名は案件ごとにばらばらなので、
  一度だけ正規化して以降を単純にする。実数値→パレットの分類もここで済ませる。
- **`gdaldem` のカラーマップに `nv` 行を書かない。** `nv` はバンドの NoData 値に
  展開されるため、ランク値より後ろに置くと「LUT が単調非減少でない」で落ちる。
  パレット経路のラスターは NoData を常に `nodata_rank`（既定0）にしているので、
  `0 0 0 0 0` という数値行で表す。
- **凡例の色は一意でなければならない。** 産総研 JSON凡例フォーマットの要求。
  `rankdef.py` が定義ファイル読み込み時に検証する。
- **ファイル間で値属性・値の種別が食い違ったらエラーで止める。**
  国土数値情報の都道府県 ZIP には想定最大規模（`A31a_205`）・浸水継続時間
  （`A31a_305`）・家屋倒壊等氾濫想定区域（`A31a_405`）が同居しており、
  意味も凡例も別物なので 1 データセットにまとめてはいけない。
- **`-makevalid` が壊れたら `-makevalid` 無しでやり直す。** 無効ジオメトリを直せる一方、
  全件落とすことも、例外で ogr2ogr ごと落ちることもある（石川県の MAXALL に閉じていない
  リングが混ざっており、GEOS が `IllegalArgumentException` を投げる）。Step 2 が
  どちらの壊れ方でも自動で戻す。**やり直すときは GPKG ごと作り直す**こと。
  ファイル単位で切り替えると、失敗した append の残骸と正常分の区別がつかず二重登録になる。
- **属性値は先頭 20 万件しか読まない。** 数百万フィーチャの入力があるため
  （`inspect_inputs.py` の `sample_limit`）。したがって検査レポートの値域・種類数・
  NULL 件数は「読んだ範囲」の値で、母数も読んだ件数で数える。全フィーチャ数を母数に
  すると打ち切った分がまるごと NULL に化ける。**タイルに書く値域は Step 4 が
  ラスター全体から取り直す**ので、検査レポートとは一致しないことがある
  （石川県 犀川で 5.731 と 6.414）。

## 調べて確認した事実（公開データ・仕様）

再調査の手間を省くためのメモ。すべて一次資料か実データで確認したもの。

- **洪水でも実数値の浸水深は公開されている。ただし都道府県管理河川に限る。**
  以前ここには「洪水の公開データに実数値は無い」と書いてあったが誤りで、
  2026-08 に調べ直して訂正した。都道府県が自らの管理河川について、電子化ガイドラインの
  `MAXALL`（最大包絡）をそのまま公開している例がある。
  - **石川県**が 27 河川ぶんをシェープファイルで公開（`MAXALL.SHP`、属性は
    `MESH` / `標高` / `浸水深`(Real,m) / `浸水深ﾗﾝｸ` / `流速` / `流速ﾗﾝｸ`）。
    数値PNG経路の検証にはこれを使う。犀川で実測: 139 万ポリゴン・5mメッシュ・
    浸水深 0.01〜6.414m・EPSG:4612。
  - **東京都**が 15 流域ぶんを CSV で公開（`図郭No,浸水深,地盤高,緯度,経度` の
    点データ・10mメッシュ・CC BY）。中川・綾瀬川圏域だけが利根川水系。
    点なのでメッシュポリゴンへの変換が要る（未実装）。
- **大河川の本川の実数値は公開されていない。** 利根川・荒川などは国管理で、
  その `MAXALL` を出しているところが無い。実際に確認した範囲:
  国土数値情報 A31a/A31b は浸水ランクコード、都道府県公開のシェープも
  ランク（群馬県の利根川は `区分` Integer の 5 ポリゴン）、ハザードマップポータルの
  オープンデータ配信はランク配色の PNG タイル、水害リスクマップは PDF のみ。
  浸水ナビ API は緯度経度指定のポイントクエリで面的取得はできない。
  **「実数値」と「大河川の本川」は公開データでは両立しない。**
- **MAXALL シェープの DBF は文字コード指定が無いことがある**（LDID=0・`.cpg` 無し）。
  GDAL が日本語のフィールド名を生バイトで返し、`VALUE_FIELD` の指定が一致しなくなる。
  `common.sh` の `resolve_src_encoding()` が指定の無いときだけ CP932 を仮定する。
  国土数値情報は LDID=19（Shift-JIS）が入っているので触らない。
- **津波でも実数値が公開されている。** 福島県が `MAXALL` 形式のまま最大浸水深
  （属性 `z`、Real、m）を公開している。同梱サンプルからは外したが、
  実数値のもう 1 つの例として使える（CC BY 2.1 JP）。
- **国土数値情報のランクコード**（公式コードリスト xlsx で確認）
  - 浸水ランク: 1=0〜0.5m / 2=0.5〜3.0m / 3=3.0〜5.0m / 4=5.0〜10.0m /
    5=10.0〜20.0m / 6=20.0m以上
  - 浸水継続時間ランク: 1=12h未満 / 2=12〜24h / 3=24〜72h / 4=72〜168h /
    5=168〜336h / 6=336〜672h / 7=672h以上
  - 危険区域区分: 1=氾濫流 / 2=河岸浸食
- **標準配色**は洪水浸水想定区域図作成マニュアル（第4版）図-7.2-1（6段階）・
  図-7.2-2/表-7.2（8段階詳細版）・表-7.4（継続時間）。国土数値情報のランクと 1:1 で対応する。
- **産総研の解説ページは、国交省「重ねるハザードマップ」の配信を
  パレットPNGタイルの適合例として挙げている。** 地理院標高タイルは数値PNG（係数0.01）、
  Mapbox Terrain-RGB も数値PNG（係数0.1・オフセット-10000）。
- **電子化ガイドラインは第5.1版（令和8年6月）が最新。** 第5版（令和6年4月）ではない。
- **国土数値情報の ZIP はエントリごとに文字コードが混在する。** 汎用フラグ bit 11 を
  見て、立っていなければ CP437 → CP932 で読み直す。ZIP 全体で一括判定すると落ちる
  （A31a-25_39_10_SHP.zip で実際に遭遇）。

## ビューワ実装の決めごと

- **タイルURLテンプレートの解決に `new URL()` を使わない。** `{z}` が `%7Bz%7D` に
  パーセントエンコードされ、以降の置換が一致せず全タイルが 404 になる（実際に踏んだ）。
  `resolveTemplate()` が文字列連結で解決する。
- **Service Worker の `controllerchange` で無条件に reload しない。** 初回訪問では
  「未制御 → activate」で必ず一度発火するため、初回に必ずページが読み直される。
  そのとき URL には `hash: true` が書いたハッシュが付いているので、
  「URL で位置指定が無い初回だけデータ範囲に合わせる」判定が壊れる（実際に踏んだ）。
  `navigator.serviceWorker.controller` の有無を先に見て、本当の更新時だけ reload する。
- **`fitBounds` は `duration: 0`。** アニメーション中に hashchange が割り込むと戻される。
- **`HAD_HASH` は Map 生成前に控える。** `hash: true` の Map は初期化直後に自分で
  ハッシュを書くため。なお esbuild が単一参照の const をインライン展開して評価順を
  ずらすことは無い（ビルド結果で確認済み）。
- **タイルの 404 は無効値として扱う。** `gdal2tiles -x` で完全透明タイルを出力しない
  ため、404 は「取得失敗」ではなく「その範囲に値が無い」を意味する。
- **ラスターは `raster-resampling: 'nearest'` 固定。** 画素値が意味を持つので補間しない。
- **背景の「地図」は PMTiles 配信。** 地理院 最適化ベクトルタイルが
  `pmtiles://` なので、ハザードタイル自体は XYZ ラスターでも
  `maplibregl.addProtocol('pmtiles', ...)` の登録が必要（外すと背景が出ない）。
- ビューワを変更したら Playwright 等で実際に描画とクリック読み取りを確認すること。
  タイルURLの不整合は「地図は出るがオーバーレイだけ出ない」形で静かに壊れる。

## 開発時の注意

- スクリプトは `set -euo pipefail` 前提。`common.sh` を `source` してから使う。
- 設定値の既定は `common.sh` の `load_config()` にまとめてある。新しい設定を足す場合は
  そこに既定値を書き、`config/sample.conf.example` にコメント付きで追記する。
- ファイル名の空白・日本語に耐えるよう、入力列挙は `find -print0` で NUL 区切りにしている。
- GDAL Python: `gdal.Open(path).GetRasterBand(1)` と書くと Dataset が回収されて
  バンドが無効になる。Dataset の参照を変数で保持すること（実際に踏んだ）。
- タイルを触る変更をしたら `tools/validate_tiles.py` を必ず通す。
  仕様違反は目視では気づけない（下位ZLの中間色など）。
